# Chart name in OCI format (spec 5.3). Published by the helm-charts repo's
# release workflow; source of the chart is
# https://github.com/shophub-project-2026/helm-charts (charts/shophub).
chart: oci://ghcr.io/shophub-project-2026/charts/shophub
version: "0.2.0"
release: shophub
namespace: shophub
createNamespace: true
